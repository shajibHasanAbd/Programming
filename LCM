#include <stdio.h>
int main(){

    int n1,n2,gcd,lcm;
    printf("enter the two number : ");
    scanf("%d %d",&n1,&n2);

    for(int i=1;
        if(n1%i==0 && n2%i==0){

        gcd = i;

        }

         }
    lcm=(n1*n2)/gcd;
    printf("LCM:%d",lcm);


    return 0;
}
